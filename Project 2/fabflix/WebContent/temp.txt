	<!--<td><%=/*movie_info.get(0)*/%></td>
	<td><%= /*movie_info.get(1) */%></td>
	<td><%=/*movie_info.get(2) */%></td>
	<td><%= /*genre_info.toString().replace("[", "").replace("]","")*/%></td>
	<td><%
			/*for(int i=0; i < star_info.size(); i = i + 2)
			{
				String name = star_info.get(i) + " " + star_info.get(i+1);
				if(!(i + 2 == star_info.size()))
					name = name + ",";
			*/%>
			<%=/*name*/%>
			<%//}


	%></td>-->

	         /*
        String star_joins = "FROM stars INNER JOIN stars_in_movies ON stars.id = stars_in_movies.star_id "+
					"INNER JOIN movies ON movies.id = stars_in_movies.movies_id WHERE movie.id =?";
		String genre_joins = "FROM movies INNER JOIN genres_in_movies ON movies.id = genres_in_movies.movie_id "+
					"INNER JOIN genres ON genres.id = genres_in_movies.genres_id WHERE movie.id=?";*/


							/*System.out.println("Movie query is " + queries.get(0));
		System.out.println("star query is " + queries.get(1));
		System.out.println("Genre query is " + queries.get(2));*/